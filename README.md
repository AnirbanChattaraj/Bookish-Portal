# 📚 Bookish – Registration Form (HTML Only)

## 📖 About
Bookish is a simple **HTML-only project** created to practice and demonstrate core HTML concepts such as semantic tags, forms, navigation, and lists.  
It contains two pages:
- `index.html` → Homepage with categories and intro.  
- `forms.html` → Registration form for book lovers.  

---

## 🚀 Features
- Semantic HTML structure (`<header>`, `<main>`, `<aside>`, `<footer>`)  
- Navigation links between pages  
- Registration form with:
  - Username input  
  - Gender selection (radio buttons)  
  - Email input (validated with `type="email"`)  
  - Book category drop-down  
  - Submit button  
- Clean and accessible HTML, no CSS or JavaScript used

- Preview>>https://anirbanchattaraj.github.io/Bookish-Portal/
